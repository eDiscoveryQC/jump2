/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Add any other stable config options you need here

  // Example: enable bundle analyzer if you want
  // Uncomment and install @next/bundle-analyzer if used
  /*
  webpack(config, { isServer }) {
    // custom webpack config here
    return config;
  },
  */

  // Other stable experimental features can go here if officially supported
};

module.exports = nextConfig;
